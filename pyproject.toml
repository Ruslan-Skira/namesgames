line-length=200
target-version = ['py36', 'py37', 'py38']
exclude = '''

(
  /(
      \.eggs         # exclude a few common directories in the
    | \.git          # root of the project
    | \.hg
    | \.mypy_cache
    | \.tox
    | \.venv
    | _build
    | buck-out
    | build
    | dist
    | wsgi.py
  )/
  | \namesgames\/wsgi.py\          # also separately exclude a file named foo.py in
                     # the root of the project
)
'''